import 'package:flutter/material.dart';

//Colors for logo gradients
const topLogoColor = Color.fromARGB(255, 122, 118, 5);
const bottomLogoColor = Color.fromARGB(158, 220, 219, 206);

//Color for custom shape
const primaryColor = Color.fromARGB(236, 248, 249, 226);

//Text colors
const blackColor = Colors.black;
